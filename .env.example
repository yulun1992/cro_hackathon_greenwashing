# Optional environment variable names read by the notebook.
# This file is a reference, not loaded automatically. Prefer Databricks secrets.
AZURE_OPENAI_CHAT_URL=
AZURE_OPENAI_DEPLOYMENT=
AZURE_OPENAI_API_KEY=
# AZURE_OPENAI_CHAT_URL must be the FULL approved chat/completions URL.
# Include the API version supplied by your team if the endpoint requires it.
# No real credentials should be placed in GitHub or notebook source.
